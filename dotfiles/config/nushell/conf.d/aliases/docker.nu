export alias dk   = docker
export alias dkc  = docker container
export alias dkcr = docker container run
export alias dkcs = docker container stop
export alias dkcet = docker container exec -it
export alias dkcls = docker container ls

export alias dkv  = docker volume
export alias dkvc = docker volume create
export alias dkvr = docker volume remove
export alias dkvls = docker volume ls