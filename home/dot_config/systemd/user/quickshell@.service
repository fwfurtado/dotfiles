[Unit]
Description=Quickshell config %i
PartOf=graphical-session.target
After=graphical-session.target

[Service]
Type=simple
ExecStart=/usr/bin/qs -c %i
Restart=on-failure
RestartSec=1

[Install]
WantedBy=graphical-session.target
